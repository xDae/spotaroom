import React from 'react';

const Sidebar = ({}) => (
	<div className="sidebar">
		<h2 className="h2-header">Filters</h2>
	</div>
);

// Sidebar.propTypes = {
// 	: React.PropTypes.
// };

export default Sidebar;
